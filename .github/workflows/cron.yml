name: 5-Minute Cron Reminders

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Call Reminders API
        run: curl -s https://avtosalon-puce.vercel.app/api/cron/reminders
